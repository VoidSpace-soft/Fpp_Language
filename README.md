## Fpp Language
Compiler and toolkit for F++. <br>

## Install
for debian/ubuntu download the .deb given and do `sudo apt install ./fpp-1.0lar.deb`. <br>

## Compile script
Write your script in a file with an extension of your choice. and compile using `fpp <input file> <output file>`
